# Copy to .env and fill in to enable the real LLM path.
# If ANTHROPIC_API_KEY is absent, second-brain-os runs in deterministic MOCK mode
# (offline, no network) using a local hashing embedder. Tests and the demo run
# fully in mock mode with zero secrets.

ANTHROPIC_API_KEY=
MODEL_ID=claude-opus-4-8
